version: '3.9'

services:
  backend:
    build: ./backend
    container_name: inventario-backend
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    container_name: inventario-frontend
    ports:
      - "80:80"